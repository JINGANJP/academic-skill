interface:
  display_name: "Academic Research"
  short_description: "Interactive academic workflow that stays lightweight by default and goes deeper only when the task requires it"
  default_prompt: "Use $academic-research to align the task, choose the lightest suitable workflow, build enough verified literature and evidence, and then draft a paper, thesis section, proposal, or literature review."
